[tool.poetry]
name = "ssh-moduli-builder"
version = "0.1.0"
description = ""
authors = ["austinRon <becker.wiliams@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
